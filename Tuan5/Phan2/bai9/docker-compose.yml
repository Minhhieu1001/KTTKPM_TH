version: '3.8'

services:
  flask_app:
    build: ./app
    container_name: flask_app
    ports:
      - "5000:5000"
    environment:
      ENV: production
      FLASK_APP: app.py
    restart: always
    volumes:
      - ./app:/app
